# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rails secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

# Shared secrets are available across all environments.

# shared:
#   api_key: a1B2c3D4e5F6

# Environmental secrets are only available for that specific environment.

development:
  secret_key_base: a651d6b859149aa2267370ce80d6f5b88e864f41eba72252daa2cf33639c96c131ac30c3e514b69f1d1a5a001dc435a3cc46c66196706027b424d70304724774
  OAUTH_LOGIN: 1002781387609-jktlp53bku8gcffp83ejdv1a40vqscea.apps.googleusercontent.com
  OAUTH_SECRET: I-DZDlBBLH9sr02AJKxl_img

test:
  secret_key_base: a96a5cb614e61b38d59b41f9b9cf552ac024eb8e260ed1201bb03ef8efab7545ac1aa840da2984f93b9ac30d77db43e595bcc812d16561b20f036e656e919fae

# Do not keep production secrets in the unencrypted secrets file.
# Instead, either read values from the environment.
# Or, use `bin/rails secrets:setup` to configure encrypted secrets
# and move the `production:` environment over there.

production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
  OAUTH_LOGIN: <%= ENV["OAUTH_LOGIN"] %>
  OAUTH_SECRET: <%= ENV["OAUTH_SECRET"] %>
  # APPLICATION_CONFIG_SECRET_TOKEN: <%= ENV["APPLICATION_CONFIG_SECRET_TOKEN"] %>
